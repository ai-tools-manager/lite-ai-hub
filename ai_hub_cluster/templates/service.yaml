apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai_hub_cluster.fullname" . }}
  labels:
    {{- include "ai_hub_cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai_hub_cluster.selectorLabels" . | nindent 4 }}
